language: node_js
node_js:
  - "8"
install:
  - npm install
script:
  - npm run lint
  - npm run compile

before_deploy:
  - rm .gitignore
  - ls -l ./

deploy:
  provider: npm
  email: $NPM_EMAIL
  api_key: $NPM_TOKEN
  skip_cleanup: true
  on:
    branch: master
